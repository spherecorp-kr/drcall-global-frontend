# API Base URL - Patient Service
VITE_API_BASE_URL=http://localhost:18081

# Development: Use mock API instead of real backend (true/false)
# When enabled, MSW will intercept API calls and return mock data
# This also controls whether services use mock or real implementations
VITE_USE_MOCK_API=false
